resources:
  - ../base

helmCharts:
  - name: cilium
    repo: https://helm.cilium.io/
    version: 1.19.1
    releaseName: cilium
    namespace: kube-system
    valuesFile: ../base/cilium-values.yaml

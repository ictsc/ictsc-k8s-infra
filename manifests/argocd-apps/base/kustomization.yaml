resources:
  - infra

patches:
  - target:
      kind: Application
    patch: |-
      - op: add
        path: /spec/syncPolicy/syncOptions/-
        value: ServerSideApply=true
      - op: add
        path: /spec/syncPolicy/automated/enabled
        value: true

---
- name: Check if swap file exists
  ansible.builtin.stat:
    path: "{{ swap_file_path }}"
  register: swap_file_stat

- name: Create swap file
  ansible.builtin.command:
    cmd: fallocate -l {{ swap_file_size_gb }}G {{ swap_file_path }}
    creates: "{{ swap_file_path }}"
  when: not swap_file_stat.stat.exists

- name: Set swap file permissions
  ansible.builtin.file:
    path: "{{ swap_file_path }}"
    mode: "0600"
    owner: root
    group: root

- name: Format swap file
  ansible.builtin.command:
    cmd: mkswap {{ swap_file_path }}
  when: not swap_file_stat.stat.exists
  register: mkswap_result
  changed_when: mkswap_result.rc == 0

- name: Set vm.swappiness
  ansible.builtin.sysctl:
    name: vm.swappiness
    value: "{{ swap_swappiness }}"
    state: present
    sysctl_set: true
    sysctl_file: /etc/sysctl.d/99-swappiness.conf
    reload: true

- name: Deploy systemd swap unit file
  ansible.builtin.template:
    src: swapfile.swap.j2
    dest: /etc/systemd/system/swapfile.swap
    mode: "0644"
    owner: root
    group: root
  notify:
    - Reload systemd
    - Restart swap

- name: Enable and start swap
  ansible.builtin.systemd:
    name: swapfile.swap
    enabled: true
    state: started
    daemon_reload: true
